import React, { Fragment } from "react";

const Profile = () => (
  <Fragment>
    <h1 className="title">Remove profile</h1>
    <button className="button is-danger">Delete</button>
  </Fragment>
);

export default Profile;